# Аудит проекта ai-helper-bot

Дата: 2026-07-05

## Итог

Это бот-советник/журнал сделок (BingX API), а не бот, который сам торгует — в `services/bingx_api.py` нет эндпоинтов на открытие/изменение ордеров, только чтение баланса, позиций и рыночных данных. Это снижает риск прямой потери денег из-за багов, но AI-рассчитанные риск/SL/TP всё равно показываются человеку, который может действовать по ним — так что ошибки в расчётах остаются серьёзными.

Три самых срочных проблемы:

1. `webhook_server.py` делает `git reset --hard` + рестарт сервиса по любому push с валидной подписью `WEBHOOK_SECRET` — утечка одного секрета даёт полный удалённый доступ к серверу (RCE).
2. `services/calc_engine.py` дважды применяет плечо при расчёте размера позиции — реальный риск может оказаться в 10–100 раз больше заявленного процента.
3. API-ключи BingX хранятся в SQLite в открытом виде, без шифрования.

В целом качество кода выше среднего для такого проекта: единообразное логирование, retry/timeout на сетевых вызовах, WAL-режим SQLite с блокировками, продуманный "деградированный" режим в AI consensus engine. Но есть реальные, непроверенные арифметические баги в торговых движках — их стоит исправить, прежде чем доверять цифрам.

---

## 1. Безопасность

| Файл:строка | Критичность | Проблема | Как исправить |
|---|---|---|---|
| `webhook_server.py:11,48-51` | Критично | Эндпоинт деплоя выполняет `git fetch && git reset --hard origin/main` и `systemctl restart` при любом push с валидной HMAC-подписью `WEBHOOK_SECRET`. Нет IP-allowlist, нет rate limiting. Утечка секрета = RCE на сервере. | Ограничить доступ по IP GitHub, запускать сервис от непривилегированного пользователя, ротировать секрет. |
| `services/database.py:83-84,201-202,305-316` | Высокая | `bingx_api_key`/`bingx_secret_key` хранятся как обычный `TEXT` в SQLite без шифрования. | Шифровать секреты (например, Fernet с ключом из env) перед сохранением. |
| `.env.example` / `bingx_api.py:10-11` | Средняя | Нет проверки при старте — если ключ не задан, запросы уйдут с пустым секретом, вместо явной ошибки запуска. | Падать при старте, если обязательные ключи не заданы (как уже сделано для `TELEGRAM_BOT_TOKEN`). |
| `ai/providers/groq_provider.py:16` | Низкая | Захардкожен прокси `socks5://127.0.0.1:1080` для каждого запроса к Groq — похоже на забытый артефакт разработки. | Сделать прокси настраиваемым через env, по умолчанию — без прокси. |
| `services/database.py` (весь файл) | ✅ хорошо | Все SQL-запросы параметризованы (`?`), инъекций не найдено. | — |
| Весь репозиторий | ✅ хорошо | Нет `eval`/`exec`/`pickle`. `.gitignore` корректно исключает `.env*`, `*.db*`, `venv/`. | — |
| `groq_provider.py:111` | Низкая | В логи попадают полные тела ответов об ошибках (до 500 символов) — могут содержать данные, идентифицирующие аккаунт. | Обрезать сильнее / ограничить доступ к файлам логов. |
| `handlers/system.py:21-22`, `bot.py:45` | Средняя | Проверка админа — просто `chat_id == CHAT_ID` из env, без rate limiting и доп. подтверждения для деструктивных операций (`restore_history_command`). | Приемлемо для одного пользователя; при мультипользовательском режиме нужно перейти на систему `user_id` из БД. |

## 2. Финансовая/торговая логика

| Файл:строка | Критичность | Проблема | Как исправить |
|---|---|---|---|
| `services/calc_engine.py:19-21` | Критично | `position_size = margin * leverage / price`, где `margin` уже включает `leverage` — плечо применяется дважды. При balance=$1000, risk=1%, leverage=10x реальный размер позиции завышен в leverage раз (100x риска вместо 10x). | Считать `position_size = risk_amount / abs(entry - stop_loss)` (риск-ориентированный расчёт) либо применять leverage один раз. |
| `calc_engine.py:30-31` vs `ai/risk_engine.py:59-63` | Средняя | Две разные формулы цены ликвидации в разных модулях — дадут разные числа для одной и той же позиции. | Свести в одну общую протестированную функцию, сверить с документацией BingX. |
| `ai/engines/scoring_engine.py:69-82` vs `judge_agent.py:53-58` | Высокая | `_calc_risk()` возвращает "чем выше — тем опаснее", но `JudgeAgent` складывает его с положительным весом, как будто выше = лучше. Опасная позиция без SL/TP и с высоким плечом может завышать итоговый score вместо понижения. | Инвертировать шкалу перед передачей в judge, либо изменить знак веса. |
| `services/stop_engine.py:43,46` | Средняя | Дефолтный SL — плоские 5% от цены входа, без учёта волатильности актива. | Масштабировать по ATR/классу волатильности. |
| `services/tp_engine.py:59-71` | Низкая | Аналогично — фиксированные % для TP без учёта волатильности. | Тот же fix. |
| `services/auto_sync.py:62-69` | Средняя | Цена выхода оценивается как `entry + pnl/qty` на основе последнего known unrealized PnL, а не реальной цены закрытия — это оценка, не факт. | Помечать в сообщениях как "оценочно", либо сверять с `get_closed_orders()`. |
| `database.py` / `bot.py:70` / `migrate_to_sqlite.py:7-15` | Низкая | Маппинг BUY/SELL→LONG/SHORT продублирован в разных местах, в `migrate_to_sqlite.py` есть недостижимая ветка кода. | Вынести в общий `ai/engines/normalizer.py`, использовать везде. |
| `ai/risk_engine.py:77-99` | Низкая | Расчёт риска портфеля не делится на leverage — по сути это риск в дистанции цены, не в марже. Не документировано как упрощение. | Задокументировать или уточнить с учётом margin mode. |
| `database.py` (много мест) | Низкая | Мультипользовательская схема (таблица `users`, ключи на пользователя) есть, но почти весь код хардкодит `user_id='default'`. | Либо довести мультитенантность до конца с тестами, либо убрать заготовку. |

## 3. Обработка ошибок и надёжность

| Файл:строка | Критичность | Проблема | Как исправить |
|---|---|---|---|
| `services/market_data.py:55` | Высокая (пока не активна) | `api_cache.set(..., ttl=ttl)` — метод `APICache.set()` не принимает `ttl`, будет `TypeError`. Сейчас недостижимо, т.к. `/analyze` не зарегистрирован как команда, но сломается, если его включат. | Добавить поддержку `ttl` в `APICache.set()` перед включением `/analyze`. |
| `migrate_to_sqlite.py:29,39` | Высокая | `Database.add_open_trade({...})` вызывается как метод класса, а не экземпляра — упадёт с `TypeError` при любом запуске скрипта. | Создать `db = Database()` и вызывать `db.add_open_trade(...)`. |
| `services/bingx_api.py:32-39` | Средняя | Сетевые и бизнес-ошибки API смешаны в одном поле `code=-1` — хрупко для будущих изменений API BingX. | Ввести отдельный флаг `transport_error`. |
| `bingx_api.py:176-178` | Низкая | Исключение при обогащении позиций TP/SL глушится молча (`pass`), без логирования. | Логировать на уровне warning. |
| `ai/providers/groq_provider.py:143-148` | Средняя | При исчерпании ретраев возвращается строка `"AI analysis unavailable: ..."` как будто это валидный ответ модели — не все вызывающие места проверяют этот сентинел. | Кидать исключение вместо строки-заглушки. |
| `core/scheduler.py:28-37` | Низкая | Нет backoff при затяжном падении BingX/Groq — бот будет долбить недоступный API каждые 15 секунд. | Добавить экспоненциальный backoff/cooldown после N ошибок подряд. |
| `services/auto_sync.py:174-178` | ✅ хорошо | Корректная блокировка (`asyncio.Lock`) от параллельных синхронизаций, защита от преждевременного закрытия позиции при временном сбое API. | — |
| `services/database.py:60,231-238` | ✅ хорошо | WAL-режим, `busy_timeout`, retry-декоратор на `OperationalError` — разумный подход к потокобезопасности. | — |

## 4. Качество кода

| Файл:строка | Критичность | Проблема | Как исправить |
|---|---|---|---|
| `services/trading_storage.py` (весь файл) | Низкая | Мёртвый код — JSON-хранилище сделок, нигде не используется, вытеснено `database.py`. Есть и стухший `data/trading.json`. | Удалить, если не нужен для отката. |
| `services/ai_trading.py` | Низкая | Использует `print()` вместо `logging`, в отличие от всего остального кода. | Заменить на `logger.info/error`. |
| `services/database.py` (671 строка) | Низкая | God-object: схема, пользователи, поведенческие события, сделки, статистика, key/value-хранилище — всё в одном классе. | Разбить на `UserRepository`, `TradeRepository`, `MemoryStore`. |
| `ai/context_builder.py` (437 строк) | Низкая | Тоже God-object, плюс словарь `trader_profile` продублирован идентично в трёх методах. | Вынести в константу модуля, разбить класс. |
| `core/router.py:47-63` и `auto_sync.py:72-88` | Низкая | `_format_verdict()` продублирована дословно в двух файлах. | Вынести в общий модуль. |
| `handlers/trading.py`, `system.py`, `ai.py` | Низкая | Хелперы `_clean()` и `_send_long()`/`_send_chunks` продублированы (с небольшими различиями) в нескольких обработчиках. | Консолидировать в `utils/`. |
| Весь репозиторий | Низкая | Мало тайп-хинтов вне `calc_engine.py`/`risk_engine.py` — именно там, где найдены реальные баги (`ttl=`, unbound `add_open_trade`), mypy/pyright, вероятно, поймал бы их раньше. | Добавлять тайп-хинты постепенно, начиная с финансовых движков; добавить mypy в CI. |
| `core/router.py:18-19` vs `core/container.py` | Низкая | Роутер создаёт `Database()`/`AITradingAnalyzer()` напрямую вместо использования контейнера, который сам же импортирует для других зависимостей. | Использовать `container.get_db()`/`get_ai_analyzer()` везде. |

## 5. Архитектура

- `core/container.py` — простой service locator (не настоящий DI), но рабочий для масштаба проекта.
- `core/router.py` — маршрутизация callback-кнопок через цепочку `if/elif`; читаемо для ~10 типов, но содержит бизнес-логику (`generate_full_ai_analysis`), которая дублирует то, что уже делает `auto_sync.py:_analyze_and_notify`.
- Разделение `handlers/` (Telegram-специфика) и `services/`+`ai/` (бизнес-логика) в целом выдержано хорошо.
- Пакет `ai/` организован чисто: `agents/` / `engines/` / `providers/` — разумное разделение. `ConsensusEngine` с таймаутами по агентам, деградированным режимом и реальной статистической метрикой разногласия — качественное решение, одна из сильных сторон проекта (омрачается багом со знаком risk_score, см. раздел 2).
- `webhook_server.py` архитектурно не связан с остальным ботом — отдельное Flask-приложение для деплоя. Учитывая его высокие привилегии (`git reset --hard` + рестарт), логичнее вынести в отдельный ops-репозиторий.

## 6. Зависимости

| Пакет | Версия | Комментарий |
|---|---|---|
| `python-telegram-bot[job-queue]` | `==20.7` | Релиз конца 2023 — на середину 2026 заметно устарел, стоит проверить актуальные советы по безопасности и обновиться. |
| `httpx[socks]` | `==0.27.0` | Начало 2024, есть более новые релизы — проверить advisories. |
| `requests[socks]` | `==2.31.0` | Версия-фикс для CVE-2023-32681 (утечка `.netrc` при редиректе) — сам фикс уже включён, но стоит проверить более поздние CVE. |
| `flask` | `==3.0.0` | Самый чувствительный пакет с точки зрения безопасности, т.к. `webhook_server.py` смотрит наружу — проверить advisories Flask/Werkzeug после 3.0.0. |
| `python-dotenv` | `==1.0.0` | Риск минимален. |
| `groq` | `>=0.9.0` | Нижняя граница без верхней — риск непредсказуемых обновлений. |
| `aiohttp` | `>=3.9.0` | Та же проблема; у aiohttp исторически были CVE (например, request smuggling) — стоит зафиксировать точную версию. |

**Рекомендация**: зафиксировать точные версии всех пакетов (заменить `>=` на `==`) и прогнать `pip-audit`/`safety check` — этот аудит не может достоверно подтвердить текущий статус CVE без прямого запроса к базам PyPI/OSV.

---

*Аудит подготовлен на основе полного чтения ai/, services/, core/, handlers/, bot.py, webhook_server.py, requirements.txt, .env.example, .gitignore, migrate_to_sqlite.py (~7500 строк, 50 файлов).*

---

## Статус на 2026-07-09 (для Этапа 1 плана AI Trading Core)

Коммит `8dacd1d` ("Fix critical bugs from audit") закрыл все пункты "Критично" и почти все "Высокая"/"Средняя" из разделов 1–3. Ниже — что сделано и что реально осталось, чтобы считать Этап 1 ("Завершить фундамент") выполненным.

### Исправлено коммитом 8dacd1d

- **Безопасность**: `webhook_server.py` — добавлены IP-allowlist (диапазоны GitHub Hooks), rate-limit на неверные подписи, проверка `repository.full_name`, лимит размера тела запроса; API-ключи BingX теперь шифруются в БД (`services/crypto_utils.py`, Fernet).
- **Торговая логика**: двойное применение плеча в `calc_engine.py` устранено; инвертирована шкала `risk_score` в `scoring_engine.py`/`judge_agent.py` (раньше опасная позиция могла завышать итоговый score); формула цены ликвидации и классификация волатильности сведены в один модуль `utils/liquidation.py` (использовался в двух местах с разными формулами); SL/TP fallback теперь масштабируется по волатильности актива вместо плоских %.
- **Надёжность**: `APICache.set()` поддерживает `ttl`; `migrate_to_sqlite.py` больше не падает (вызов через инстанс, try/except на сделку, синтетический `orderId`); транспортные и бизнес-ошибки BingX API разделены флагом `_transport_error`; `GroqProvider` теперь кидает исключение вместо строки-заглушки при исчерпании ретраев.
- **Качество кода**: удалён мёртвый `services/trading_storage.py` + `data/trading.json`; продублированные `_clean`/`_send_long`/`_format_verdict` вынесены в `utils/telegram_text.py` и `utils/formatting.py`; `core/router.py` переведён на `container.get_db()`/`get_ai_analyzer()`.

### Осталось открытым (не входило в коммит 8dacd1d)

По задачам Этапа 1 из плана:

| Задача Этапа 1 | Статус | Что осталось |
|---|---|---|
| Завершить рефакторинг | Частично | `services/database.py` (677 строк) и `ai/context_builder.py` (437 строк) остаются god-объектами. `Database()` создаётся напрямую (не через `container.get_db()`) в ~10 местах (`auto_sync.py`, `ai_trading.py`, `memory_engine.py`, `context_builder.py`, `handlers/system.py`, `comment_manager.py`, `trade_manager.py`) — каждый вызов открывает отдельное SQLite-соединение вместо переиспользования синглтона. Мультипользовательская схема наполовину: `user_id='default'` захардкожен в ~20 местах. |
| Проверить стабильность BingX API | Частично | Нет fail-fast проверки при старте — если `BINGX_API_KEY`/`BINGX_SECRET_KEY` не заданы, бот стартует и шлёт запросы с пустым секретом вместо явной ошибки (для `TELEGRAM_BOT_TOKEN` такая проверка есть в `bot.py:98`, для BingX-ключей — нет). `core/scheduler.py` опрашивает API каждые 15 сек без backoff/cooldown при затяжном сбое — при падении BingX бот будет долбить недоступный эндпоинт бесконечно. |
| Проверить корректность хранения данных | В основном ОК | WAL-режим, retry на блокировках, шифрование ключей — на месте. Нет задокументированной стратегии бэкапа/восстановления `data/trading.db` (единая точка отказа — файл на диске сервера). |
| Довести до конца автодеплой | Не завершено | Единственный механизм — `webhook_server.py` (git push → `git reset --hard` + `systemctl restart`), теперь с IP-allowlist/rate-limit. Но: нет CI (в репозитории нет `.github/workflows/`) — код не прогоняется через тесты/линт перед деплоем; нет health-check после рестарта; нет отката при неудачном деплое (если новый код не стартует — сервис просто падает на проде). |
| Провести полный аудит кода | Готово | Этот документ + текущая сверка. |

Отдельно, вне таблицы задач Этапа 1, но по-прежнему из раздела "Низкая" исходного аудита — не менялось: прокси Groq (`ai/providers/groq_provider.py:16`) всё ещё захардкожен на `socks5://127.0.0.1:1080`; `groq`/`aiohttp` в `requirements.txt` не зафиксированы (`>=` вместо `==`); `pip-audit`/CVE-проверка не проводилась.

### Рекомендация перед стартом Этапа 2 (AI Trading Core)

Ядро будет опираться на существующие `ai/agents/*`, `services/database.py` и BingX-слой как на фундамент — есть смысл закрыть до начала Этапа 2 именно то, что подрывает их надёжность, а не полноту рефакторинга:

1. ~~Fail-fast проверка `BINGX_API_KEY`/`BINGX_SECRET_KEY` при старте.~~ ✅ Сделано (`bot.py:main()`) — бот больше не стартует с пустыми ключами BingX.
2. ~~Backoff в `core/scheduler.py` при затяжных сбоях BingX/Groq.~~ ✅ Сделано — `_sync_trades_impl()` теперь возвращает флаг `api_ok`, `auto_sync_job()` по нему включает экспоненциальный backoff (15s → 300s), сбрасывается при первом успехе.
3. ~~Зафиксировать версии `groq`/`aiohttp`.~~ ✅ При проверке выяснилось, что оба пакета нигде не импортируются в коде (`GroqProvider` ходит в Groq API напрямую через `requests`, а не через официальный SDK) — это были мёртвые зависимости. Убраны из `requirements.txt` вместо пиновки; `cryptography` зафиксирован на `==49.0.0`. `pip-audit` по оставшимся пакетам (`python-telegram-bot`, `httpx`, `requests`, `flask`, `python-dotenv`, `cryptography`) ещё не прогонялся.

Разбиение `database.py`/`context_builder.py` на репозитории и доведение мультитенантности до конца можно отложить — это влияет на читаемость, но не на корректность, и естественно решится по ходу вынесения агентов в `AI Trading Core` (Этап 2), когда контракты между слоями и так придётся переопределять.

---

## Этап 2/3 плана AI Trading Core: AI Orchestrator (2026-07-09)

Добавлен `ai/orchestrator.py` — класс `AIOrchestrator`, единая точка входа в
AI Trading Core, зарегистрирован в `core/container.py` как `get_orchestrator()`.
Все точки вызова (`handlers/ai.py`, `core/router.py`, `services/auto_sync.py`)
переведены с прямого обращения к `get_consensus()`/`ConsensusEngine` на
`get_orchestrator()`.

Маршрутизация запросов (согласно плану, Этап 3):

| Тип запроса | Агенты (по плану) | Метод |
|---|---|---|
| `open_position` | Position Analyst + Risk Manager + Judge | `review_open_position()` |
| `closed_trade` | Trade Reviewer + Risk Manager + Judge | `review_closed_trade()` |
| `new_setup` | Market Analyst + Strategy Advisor + Risk Manager + Judge | `evaluate_new_setup()` |

**Важное архитектурное решение, которое стоит зафиксировать:** роли Position
Analyst / Trade Reviewer / Strategy Advisor из плана **не выделены в отдельные
классы**. Они уже существуют как режимы (`mode='open'|'post_trade'|'setup'`)
внутри `MarketAgent`/`RiskAgent`/`PsychologyAgent`, которыми управляет
`ConsensusEngine` — именно он и делает основную работу по подбору поведения
под тип запроса. Выделение их в отдельные классы прямо сейчас было бы чистым
переименованием без изменения поведения (запрещённая шаблоном
преждевременная абстракция). `AIOrchestrator` — тонкий слой поверх
`ConsensusEngine`: документирует маршрутизацию из плана, даёт единую точку
входа и логирование по типу запроса, и место, куда позже можно подставить
принципиально новых агентов (Portfolio/News/Macro Analyst — Этап 3) не
меняя вызывающий код.

**Побочная находка — исправлено в этом же проходе.** При чтении
`ai/agents/risk_agent.py` и `ai/agents/psychology_agent.py` обнаружилось, что
их методы `_analyze_open_position`/`_analyze_setup`/`_analyze_post_trade`
возвращали `risk_score`/`psychology_score` в шкале 0–10, тогда как
`JudgeAgent` и `ScoringEngine` работают в шкале 0–100. На тот момент это не
было активным багом: `ConsensusEngine._run_agents_parallel()` не использовал
эти числовые поля для `open`/`post_trade`/`setup` — реальные
`risk_score`/`psychology_score`, которые видит Judge, считает отдельно
`ScoringEngine.calculate()`, а из ответов RiskAgent/PsychologyAgent берётся
только текстовое `summary`. Но это была скрытая ловушка: 0–10 значения
выглядели валидными и лежали прямо в JSON-ответе агента — если бы в будущем
код случайно начал брать `risk_score`/`psychology_score` напрямую из ответа
агента вместо `ScoringEngine`, Judge получил бы на порядок заниженные числа
и считал бы почти любую позицию критически рискованной.

Исправление:
- `ScoringEngine._calc_risk`/`_calc_psychology`/`_calc_data_quality` сделаны
  публичными (`calc_risk`/`calc_psychology`/`calc_data_quality`) — это теперь
  единственная реализация шкалы риска 0–100 (выше = безопаснее) в проекте.
- `RiskAgent._calc_risk_score()` (открытая позиция/закрытая сделка) больше не
  дублирует логику проверки SL/TP/leverage в своей шкале — делегирует в
  `ScoringEngine.calc_risk()`.
- `RiskAgent._analyze_setup()` — свой более богатый, направленно-зависимый
  расчёт риска сетапа сохранён (он не дублирует `ScoringEngine`, а несёт
  доп. сигнал), но приведён к шкале 0–100 с той же ориентацией "выше =
  безопаснее" (раньше была обратная ориентация: выше = опаснее).
- `PsychologyAgent._analyze_setup`/`_analyze_open_position`/`_analyze_post_trade`
  — логика не дублирует `ScoringEngine` (анализ формулировок, early-exit и
  т.п.), поэтому просто пересчитана из шкалы 0–10 в 0–100 с сохранением всех
  относительных весов (все базовые значения и штрафы ×10).
- Проверено смоук-тестом: безопасная позиция (SL+TP, leverage 3x) даёт
  risk_score=95/psychology_score=100, рискованная (без SL/TP, leverage 15x) —
  risk_score=50/psychology_score=30; сетап LONG+TRENDING_UP — risk_score=70,
  LONG+UNKNOWN regime — risk_score=20. Направление шкалы везде одинаковое.

---

## Этап 4 плана AI Trading Core: анализ открытых позиций (2026-07-09)

При подготовке к Этапу 4 выяснилось, что детерминированный движок для всех
пунктов плана ("актуален ли стоп", "перенести стоп", "частично зафиксировать
прибыль", "оставить/закрыть") уже был написан ранее (`services/structure_engine.py`,
`services/stop_engine.py`, `services/tp_engine.py`, `services/ai_decision_engine.py`),
но не был подключён ни к одному живому потоку — единственный вызывающий код,
`handlers/system.py:analyze_command` (`/analyze`), нигде не зарегистрирован
как команда (см. докстринг `bot.py`: "removed /analyze and /calc from public
commands"). Это ровно та "Высокая (пока не активна)" находка про
`api_cache.set(ttl=...)` из раздела 3 исходного аудита — сам баг с `ttl`
давно исправлен, но команда так и осталась незарегистрированной.

Вместо регистрации отдельной `/analyze` (что вернуло бы фрагментацию на два
независимых способа анализа позиции, чего план явно избегает) —
`AIOrchestrator.review_open_position()` теперь дополнительно строит
`position_plan`, вызывая тот же `ai_decision_engine.analyze_decision()` на
основе живого `market_data.get_market_snapshot()`, и подмешивает его в общий
ответ. Таким образом один вызов `/consilium → Анализ позиции` теперь
закрывает весь список Этапа 4:

- качество входа и текущий риск — от AI-консилиума (Market/Risk/Psychology
  agents + Judge, как и раньше);
- актуальность стопа, рекомендация по переносу, частичная фиксация прибыли,
  решение HOLD/EXIT/DCA/PARTIAL_TP/FULL_TP — от `position_plan`.

Технические детали:
- `analyze_decision()`/`stop_engine`/`tp_engine` ожидают нормализованные
  snake_case поля (`entry_price`, `unrealized_pnl`, ...), а не сырой ответ
  BingX API (`entryPrice`, `unrealizedPnl`, ...) — `_build_position_plan()`
  прогоняет позицию через уже существующий `normalize_position()`.
- Позиции в `/consilium` берутся из `services.bingx_api.get_open_positions()`
  (живые данные биржи), а не из `db.get_open_trades()` — поэтому поля Trade
  Management Engine v2 (`dca_count`, `invalidation_sl`, `tp_zones`, `idea`,
  задаются через `/setidea`) у них отсутствуют. `_build_position_plan()`
  пробрасывает эти поля, если они есть на объекте, иначе `analyze_decision()`
  использует безопасные дефолты (без DCA-рекомендаций, инвалидация — по
  структуре рынка, а не ручной отметке). Это не регрессия — так же вело себя
  бы прямое использование `get_open_positions()` и раньше; полное объединение
  источников позиций (BingX API + DB) — отдельная, более крупная задача, не
  делалась в этом проходе.
- `handlers/system.py:analyze_command` (`/analyze`) намеренно не тронут и не
  удалён — теперь он избыточен (то же самое, но на других данных и без AI-
  консилиума), но его никто не просил менять. Кандидат на удаление в
  отдельном проходе по чистке кода.
- Проверено смоук-тестом на синтетических свечах с восходящим трендом:
  `position_plan` корректно строит `structure`/`stop`/`tp`/`decision`, а
  `handlers/ai.py:_build_response()` корректно рендерит их в ответ консилиума.

---

## Этап 5 плана AI Trading Core: анализ закрытых сделок (2026-07-09)

Автоматический разбор закрытой сделки в целом уже существовал и работал: как
только `sync_trades()` обнаруживает, что позиция закрылась, фоново
запускается `_analyze_and_notify()`, который вызывает
`AIOrchestrator.review_closed_trade()` (Trade Reviewer + Risk Manager +
Judge — качество входа/сопровождения/стопа/тейка и вердикт текстом),
сохраняет результат в `closed_trades` и шлёт пользователю уведомление. Это
уже покрывало "После закрытия бот автоматически строит разбор" и "Каждая
сделка становится частью базы знаний" (сделки со всеми AI-полями хранятся в
БД, `MemoryEngine` агрегирует win rate по тикеру/направлению).

Найдена и исправлена реальная нестыковка между двумя точками входа в один и
тот же анализ:

- `services/auto_sync.py` (автоматический путь) отдельно считал
  `TradeScorer.score(trade)` — структурированную оценку 0-10 по компонентам
  (R:R, плечо, риск на сделку, дисциплина, психология) — и сохранял только
  `total_score` как `ai_score`. Сама детализация (`details`, `verdict`)
  вычислялась и терялась.
- `core/router.py:generate_full_ai_analysis` (кнопка "Полный AI-анализ",
  ручной перезапуск) вообще не считал `TradeScorer` — вместо этого читал
  `analysis.get('ai_score')` из ответа консилиума, где такого ключа никогда
  не было (`ConsensusEngine._run_agents_parallel()` его не возвращает). При
  ручном перезапуске анализа оценка в БД просто не обновлялась — тихо, без
  ошибки, так как `ai_score` там `None` не проходило проверку `allowed` в
  `update_trade_metrics` пустым списком.

Исправление, тем же способом, что и Этап 4 (расширение общего входа вместо
дублирования в каждом вызывающем месте):

- `AIOrchestrator.review_closed_trade()` сам считает `TradeScorer.score(trade)`
  и кладёт в `result['score_breakdown']` — теперь единственный источник этой
  оценки для обоих вызывающих мест.
- `services/database.py`: добавлена колонка `closed_trades.score_breakdown`
  (TEXT, JSON) через существующий safe-migration механизм; добавлена в
  `allowed` список `update_trade_metrics()`.
- `utils/formatting.py`: новая `format_score_breakdown()` — форматирует
  `score_breakdown` в читаемый текст с русскими подписями по компонентам
  (по тому же паттерну, что и `format_verdict`, чтобы не дублировать
  форматирование в `auto_sync.py` и `router.py` по отдельности).
- `services/auto_sync.py` и `core/router.py`: оба теперь берут
  `analysis['score_breakdown']`, сохраняют его в БД вместе с `ai_score`, и
  показывают пользователю через `format_score_breakdown()`.
- Проверено смоук-тестом: `review_closed_trade()` на тестовой сделке
  (SL/TP заданы, RR≈2, leverage 3x, exit_comment заполнен) даёт
  `total_score=8.2, verdict="Отличная сделка"` с детализацией по всем 5
  компонентам; `update_trade_metrics()` с новым полем `score_breakdown`
  корректно проходит через миграцию схемы и читается обратно из БД.
